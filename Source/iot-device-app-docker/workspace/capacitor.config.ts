import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.bayer.ph.passlinkmobiledev',
  appName: 'iot-device-app',
  webDir: 'www'
};

export default config;
